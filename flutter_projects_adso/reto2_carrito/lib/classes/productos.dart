// Clase producto: id, nombre, imagen, cantidad y precio
class productos {
  final id;
  final nombre;
  final imagen;
  int cantidad;
  int precio;

  productos(this.id, this.nombre, this.precio, this.cantidad, this.imagen);
}
